* option
- value: transactionManager name
- rollbackFor
  - default 
    - rollback: RuntimeException, Error subclass
    - commit: Exception subclass
  - setup specific Exception rollbackFor
    - (rollbackFor = Exception.class)
- noRollbackFor
- propagation
- isolation: default db isolation level
- timeout
- label
- readOnly
  - framework
    - Jdbctemplate: throw Exception when write
    - JPA: not flush. not snapshot creation
  - jdbc driver
    - depend on driver version and database
    - throw Exception when write
    - read(slave), write(master). get connection to slave when read
  - database
    - internal optimization
  - caution: readonly option transmission(need test)